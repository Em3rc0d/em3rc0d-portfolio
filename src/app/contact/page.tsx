import { SectionFoundation } from "@/components/shell/section-foundation";

export const metadata = { title: "Contact" };

export default function ContactPage() {
  return (
    <SectionFoundation
      id="CONTACT"
      title="Have a system worth understanding?"
      description="The final contact surface will be intentionally small: what are you trying to build, understand, recover, or improve?"
      status="SLICE 06 / NOT YET BUILT"
    />
  );
}
